# Chess Game

A local chess game implementation in Python 3, without external dependencies (no library installations needed). 

GUI built with tkinter.

## Requirements 
Python 3.7+

## Installation
Open your command line and run the following lines
```
git clone https://github.com/AlKatse/Chess_game
cd Chess_game
python chess_game.py
```
